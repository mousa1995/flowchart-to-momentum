## satisfies

what is said => satisfies is a typescript type checking that makes a mock gets checked against a pre defined type

satisfies => a TypeScript type-checking operator [what is it part] --- checks a value against a predefined type [what does] --- const user = { name: "Mousa" } satisfies User [how looks like]
