## Dependency Injection

what i said about dependency injection => dependency injection is for a call\object that needs something and that somethin is what we give to it as dependency injection

Dependency Injection => a software design technique [what is it part] --- gives an object its dependency from outside [what does] --- used when an object needs another object [where/when] --- new TaskService(logger) [how looks like]
