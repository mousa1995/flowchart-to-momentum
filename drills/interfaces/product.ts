interface Product {
    id: string;
    name: string;
    price: number;
}

const product : Product = {
    id: 'prod_01',
    name: 'first_product',
    price: 1
}

console.log(product);

// E:\cmder\flowChart (main -> origin)
// λ npx tsx drills\interfaces\Product.ts
// { id: 'prod_01', name: 'first_product', price: 1 }

// E:\cmder\flowChart (main -> origin)