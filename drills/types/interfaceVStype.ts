// 1. interface برای تعریف قرارداد Object خیلی طبیعی است

// 2. type انعطاف بیشتری در ساخت Typeهای ترکیبی دارد

// مثلاً Union:

// type Status = "idle" | "loading" | "success" | "error";

// این را نمی‌توانی به همان شکل با interface تعریف کنی.

